# Редактирование себестоимости: локальный пакет 17 сентября

## Уточнение UI и пустых полей

В ячейке стоимости оставлен синий input без повторной кнопки-подписи
«Себестоимость…». Accessible label сохранён; загрузка по фокусу, явное
сохранение Enter/✓ и сообщения ошибок остаются. 16 scoped frontend tests
PASS (включая Chromium ввод/сохранение и цвет границы), typecheck PASS.
Это не закрывает отсутствие canonical mapping для вновь загруженного каталога.

Одиночный read-only POST WB goods/filter вернул 200, но в проверенном товаре
size-поля только price/discountedPrice/clubDiscountedPrice/sizeID/techSizeName.
Покупательских цен СПП/кошелька в этом ответе нет; Клуб не подменяет кошелёк.
Источник baskets не включался в предыдущий локальный импорт: это причина
пустых корзин, а не доказательство нулевого спроса. В этом UI-пакете новая
загрузка воронки не запускалась.

## Реальные данные WB: переключение 17 сентября

По отдельному запросу владельца выполнено чтение WB сохранённым токеном.
Локальный API теперь запускается с `--wb-readonly-data` и использует
`.local-preview/wb-readonly.sqlite`. Исходная `preview.sqlite` сохранена.
Перенесена только локальная авторизация выбранной организации: демо-каталог,
себестоимость, налоговые политики, стратегии и mapping не переносились.

Через существующий `refresh_wb_data_sources` загружены 3426 товаров/цен,
3416 карточек, остатки (1566 агрегатов), заказы/продажи (727 агрегатов),
финансы (799 агрегатов) за 10–16 сентября. Разница количества карточек
и товаров не скрывается и пока не исследована. Это ingestion, не независимая
финансовая сверка. Реальная прибыль без подтверждённых costs/policy неизвестна.

`ops/load_local_wb_readonly.py` — явный однократный запуск существующего
загрузчика с точным allowlist read endpoints, без redirects, scheduler или
внешних ценовых действий. Повторный запуск сериализован локальным file lock.
Сервис UI/API по-прежнему блокирует исходящие соединения; это локальный
снимок, не автоматическое непрерывное обновление WB. Команда запуска API:
`arch -x86_64 ../.venv/bin/python ops/current_cost_local_preview.py --wb-readonly-data`
из backend. Данные приватные, не включать `.local-preview` в публикации.

Проверено: 14 allowlist-тестов PASS, login 200, authenticated SKU API 200,
total=3426, первая страница 150, DEMO-строк нет, frontend/health 200.
Полная browser QA и дальнейшая интеграция cost mapping остаются отдельной работой.

## Дополнение: локальный каталог

В выделенный тестовый аккаунт добавлены 8 DEMO-товаров через существующие
catalog/costs/source-cache таблицы. Файл ops/seed_current_cost_preview.py
жёстко использует изолированную preview.sqlite, не переносит реальные товары
и не перезаписывает введённые версии себестоимости при повторном запуске.
Подготовлены синтетические периоды 1/7/14/30/90 дней, включая UI «по вчера».
Режим расчёта использует сохранённые источники, не legacy fake-рандомизацию;
внешние сокеты остаются заблокированы. Поправлена совместимость raw-SQL дат
кэша с SQLite. Тестовый налог относится только к выделенной DEMO-организации.
Фактически проверено: authenticated SKU API 200, 8 строк; current-cost API
200, стоимость 23000 копеек. Chromium: 8 видимых строк, AA/BA/BB/CC, карточка
маржи и поле стоимости. Снимок frontend/output/playwright/all-products/local-demo-catalog.png.
Синтетическая сводка 7 дней: выручка 336000 ₽, маржа 150108 ₽. Это не данные WB.
22 scoped backend-теста PASS. Старый текст о пустом каталоге ниже устарел.

Работа только в текущей Downloads-копии, без Git/push/deploy/production.

## Реализовано

- Существующий CostsService: серверная дата изменения, immutable версия,
  expectedCostVersionId, повтор по sourceReference, автор membership.
  Блокировка CatalogSku сериализует записи PostgreSQL; локальный SQLite editor
  использует BEGIN IMMEDIATE. Исторический API также блокирует строку SKU.
- Catalog API current-cost: права catalog/costs, организация, account scope
  для всех затронутых связанных offers. Legacy-таблица без account identity
  допускается только при единственном connected WB-аккаунте. Неоднозначные
  mapping/размеры блокируются, произвольного создания mapping нет.
- Колонка «Себестоимость, ₽/шт.» и CurrentCostEditor. Загрузка значения по
  запросу пользователя, без 150 фоновых запросов при открытии страницы.
  Enter/кнопка, Escape, точные копейки, серверное подтверждение, retry key,
  сообщение о конфликте с сохранением ввода, подтверждение shared cost.
- После записи запрос существующей страницы/summary, без обращения к WB.
- Дневной исторический расчёт не применяет внутридневную правку ко всем
  продажам дня: расхождение opening/closing cost version оставляет COGS unknown.

## Карта потребителей и незавершённость

| Потребитель | Источник и статус |
|---|---|
| Новый input | CatalogCostVersionRow через CostsService, текущий момент |
| Settlement summary/ABC | legacy_tax → CostsService, датированные дневные units; внутридневная неопределённость блокируется |
| Canonical P&L/ABC | существующий CostsService; политика расчёта отдельная, не объявлена эквивалентной новому settlement |
| Legacy стратегия/минимальная цена | settings.cogsKopecks; перенос на общую current-cost projection НЕ завершён |
| Legacy статистика, симулятор, XLSX | нужна дальнейшая проверка/перенос; не утверждать сквозной parity |

Следовательно, ввод ещё нельзя считать полностью интегрированным во все
финансовые/торговые consumers. Production-выпуск запрещён до закрытия этого разрыва.
Нет полной browser-проверки конфликта/перезагрузки, неизвестных аккаунтов,
mobile/200%; нет PostgreSQL concurrency-прогона. Отдельно остаются заказы ₽,
inSale по размерному варианту, совместные комментарии и fake-стратегии.
Чужие/реальные данные не менялись, реальные финансовые суммы не сверялись.

## Проверки

- Backend четыре scoped файла: 63 PASS. Затем добавлена проверка двух
  одновременно редактирующих SQLite sessions: cost editor файл 6 PASS.
- Ввод валюты: 15 frontend cases PASS.
- Chromium table test: 20 колонок, отсутствие margin, открытие editor,
  350,50 → один POST 35050 копеек, idempotency key, expected version — PASS.
- Typecheck и production build PASS; прежние предупреждения крупных chunks.
- Проверка обнаружила crypto.randomUUID вне secure context: исправлено
  на cryptographic getRandomValues; без Math.random.

## Локальный запуск

UI: http://127.0.0.1:5177/auth/login, API: http://127.0.0.1:58017.
Оба HTTP probes вернули 200. API запущен через
`backend/ops/current_cost_local_preview.py` из этой копии.
Отдельный `.local-preview/preview.sqlite`, existing create_all_for_local_dev;
это НЕ проверка Alembic/PostgreSQL/RLS. Внешние connect запрещены, workers не
запускаются, credentials/env внешних сервисов не наследуются. Signing secret
генерируется только в памяти, при рестарте потребуется повторный вход.

Нужно зарегистрировать отдельную локальную учётную запись. Реальных аккаунтов,
токенов WB и данных здесь нет. DEMO seed и полноценная пользовательская
приёмка пока не завершены; не выдавать пустую авторизацию за готовую CRM.
Остановить только эти собственные процессы — Ctrl+C в соответствующих
терминалах; порты 5177/58017. Не останавливать прежние процессы проекта.
