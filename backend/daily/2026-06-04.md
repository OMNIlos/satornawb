- WB repricer products: live WB rejected `POST /api/v2/list/goods/filter` with `400 Invalid request parameters`; BFF catalog fetch uses `GET` with query params, while transport-level WB timeouts retry in `RealWbApiClient`.
- Local dev token override: WB repricer BFF now prefers `VELLA_WB_API_TOKEN` from startup env before the token saved in the cabinet DB, so malformed saved tokens can be bypassed during debugging.
- UI response latency fix: WB client no longer sleeps on local token-bucket waits or WB retry headers, and transport timeout retry was removed; frontend should receive WB `200/400/401/429/503` promptly instead of waiting in loading state.
- WB repricer 429 mitigation: `/api/v1/wb-repricer/sku` was paginating WB goods by 100 and dev React could duplicate the load; backend now requests up to 1000 goods per page, caches successful catalog pages for 120s, and frontend shares one in-flight products request for 30s.
- WB repricer cache redesign: normal `/api/v1/wb-repricer/sku` now reads cached goods pages from DB; WB is called only by `POST /api/v1/wb-repricer/sku/refresh?offset=...&limit=1000`, one page per click. Frontend adds refresh/next-page buttons and auto-retry countdown for WB `Retry-After`.
- Existing backend source wiring: content cards and promotions already had WB adapters; added DB source cache plus `POST /api/v1/wb-repricer/sku/refresh-content` and `/refresh-promotions`, with frontend buttons to refresh and then read from cached `/sku`.
- Repricer period sources: added stocks refresh from WB Analytics and period stats refresh from WB Statistics orders/sales. UI now has selectable period (7/14/30/60/90 days), and `/sku?periodDays=` reads cached aggregates for stock, orders, revenue and buyout.
- Verification: changed Python files compile; IDE diagnostics are clean. Full `pytest` could not run because `pytest` is not installed on host or in `vella-api`.
- Environment gotcha: running `vella-api` uses code baked into the Docker image, not live workspace files; rebuild/restart is required before validating the fix through the browser/API container.
