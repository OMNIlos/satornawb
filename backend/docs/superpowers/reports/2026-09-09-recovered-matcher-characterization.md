# Recovered browser matcher: synthetic characterization

Source: executable-only backup `comcookie.store-20260613-185140/app.js`, SHA256
`b20e29ee1fcaef4b122df709f1d51eae3a6348c69cce9e0e5668402d8f9aa610`.
Provenance manifest received from coordinator `/tmp/satorna-production-source-recovery-20260909.md`.
Older June backup, not latest production or full Python/Node prototype.

Harness `tests/source_recovery/matcher_characterization.cjs` reads ONLY app.js,
verifies exact hash, evaluates selected source sections in Node VM with synthetic
state and explicit external boundary stubs. No browser initialization/preload,
customer workbook, pool, log, IndexedDB profile, PHP execution or network occurs.
VM is a test isolation mechanism, not a security sandbox for arbitrary source.
Existing scripts/test-matching.js was NOT executed.

## Executed facts

| Source / behavior | Result |
|---|---|
| app.js:162 shortKis / use-key helpers | Synthetic full + short aliases, separator escaping to `_x001d_` confirmed |
| app.js:274 parseChzItems | Separate name/size/color with composition excluded confirmed |
| app.js:688 sticker matching | Leading zeros discarded; ANY digit group fallback admits partial match; used page excluded |
| app.js:1434 buildXlsx | One row per matched entry, quantity=3 does not expand here; headers/leading-zero order input retained at library boundary |
| app.js:1451 rememberGeneratedChzUse | Re-generation resets in-memory consumed flag; not durable idempotency |
| app.js:1463 consumeGeneratedChzOnce | Concurrent calls in same state suppressed; failed/uncertain server call resets consumed=false |
| app.js:1379 buildPdf | Actual source constants 58x40, first batch10/next11; 22 matches ->47 pages including3 dividers |

PDF test uses fake jsPDF/image/preload boundaries: page orchestration only, NOT
real font, clipping, dimensions in generated bytes, DataMatrix validation or
visual parity. XLSX test captures aoa/library call, NOT a real XLSX artifact test.
58x40 browser source is now recovered; separate Node fragment remains58x58.
Neither establishes120x75 or A4 production renderer source.

## Read-only findings not runtime proof

- app.js:72-160: IndexedDB `wb-matcher` v2, `chz-pool`/`meta`, no tenant/account
  namespacing. get/set and load/delete consumed paths span multiple transactions;
  no two-browser-writer proof. Existing profile was not opened.
- app.js:1543-1548: both PDF/XLSX download clicks schedule consumption. Canonical
  rule is opposite: download must not consume/reserve/print. Preserve as an explicit
  migration behavior change, not hidden parity claim.
- app.js:1279-1332: source PDF may be fetched during rendering/preload. Canonical
  renderer must receive frozen assets and cannot fetch live source.
- api/chz.php:109: file flock serializes callbacks, but initialization precedes
  lock; consume at140 has no org/account/membership binding in this standalone file.
- api/chz.php:179-186: used log append precedes current JSON rewrite. Failure after
  log before rewrite can leave consumed code in available pool; rollback/recovery
  must not trust current JSON alone. No PHP data path was executed.

Do not port heuristic matching as canonical Catalog resolution. Do not equate
in-memory consumed suppression with durable exactly-once allocation. Missing or
uncertain consumed history remains fail-closed, including timeout after acceptance.

## Verification and next boundaries

6 Node characterization tests PASS, exit0, no dependencies installed. New tests
characterize existing code, not TDD implementation of a new renderer.

```sh
MATCHER_SOURCE_APP_JS=/path/to/verified/code-only/app.js \
  node --test tests/source_recovery/matcher_characterization.cjs
```

Source file must match pinned SHA or suite fails rather than running unknown code.
No recovered source copied into runtime. Hash-bound fixture dependency is explicit;
CI/integration must supply code-only source, not production pool. This does not
change shared dependencies/config or permit external downloads.

Remaining: real offline renderer dependencies/assets, barcode round-trip and
selected-unit/quantity acceptance; PHP synthetic isolated runtime/concurrency;
complete production schedules/mapping/batches source. Orders persistence separately
requires exact T1 migration. Current tests do not remove these prerequisites.
Self-review removed hardcoded grouping/dimension test inputs: harness now evaluates
the actual pinned source constants/lambda so source rule changes fail the test.
