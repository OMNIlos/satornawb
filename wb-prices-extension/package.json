{
  "name": "satorna-wb-prices-extension",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/check.mjs",
    "build": "npm run check && node scripts/build.mjs"
  }
}
